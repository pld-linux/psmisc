--- psmisc/Makefile.old	Mon Mar 29 16:51:04 1999
+++ psmisc/Makefile	Mon Mar 29 16:55:47 1999
@@ -1,6 +1,8 @@
 CC=cc
 OPTIMIZE=-O
-CFLAGS=$(OPTIMIZE) -DPSMISC_VERSION=\"`cat VERSION`\" \
+LIBS=-lncurses
+INCLUDES=-I/usr/include/ncurses
+CFLAGS=$(OPTIMIZE) $(INCLUDES) -DPSMISC_VERSION=\"`cat VERSION`\" \
   -D_GNU_SOURCE \
   -Wall -Wno-parentheses -Wwrite-strings -Wpointer-arith \
   # -Wcast-align -Wconversion -g
@@ -29,7 +31,7 @@
 killall:	killall.o signals.o
 
 pstree:		pstree.c comm.h Makefile VERSION
-		$(CC) $(CFLAGS) $(LDFLAGS) -o pstree pstree.c -ltermcap
+		$(CC) $(CFLAGS) $(LDFLAGS) -o pstree pstree.c $(LIBS)
 
 install:	$(PROGS)
 #		EBINDIR is expected to exist, so we don't try to create it
